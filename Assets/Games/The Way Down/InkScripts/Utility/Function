===function intialsetup===
~ KneeCaps = 1
~ Age = 3
~ Body = 100
~ Awareness = 10
~ Strange = "Thoughts"
~ StrangeAmount = 1

===function addKneecaps(x)===
~ KneeCaps = KneeCaps + x

===function subKneecaps(x)==
~ KneeCaps = KneeCaps - x

===function addBody(x)===
~ Body = Body + x

===function subBody(x)==
~ Body = Body - x

===function addAge(x)===
~ Age = Age + x

===function subAge(x)===
~ Age = Age - x

===function addAwareness(x)===
~ Awareness = Awareness + x

===function subAwareness(x)===
~ Awareness = Awareness - x


===function ChangeStrange(x)===
~ Strange = x

===function addStrangeAmount(x)===
~ StrangeAmount = StrangeAmount + x

===function subStrangeAmount(x)===
~ StrangeAmount = StrangeAmount - x

===function reloadKnot()===
~ StrangeAmount = StrangeAmount